# URL del backend de VoicePilot AI (repo backend/, npm run dev por defecto
# en el puerto 3000). Se usa solo del lado del servidor (Server Components,
# Server Actions, Route Handlers) — nunca llega al navegador.
BACKEND_URL=http://localhost:3000
